# ── Singularity API (Vercel serverless) ──
numpy>=1.26.0
scipy>=1.12.0
pandas>=2.1.0
yfinance>=0.2.40
scikit-learn>=1.4.0
statsmodels>=0.14.0

# ── IB Bridge (local machine, run separately) ──
# pip install ib_insync flask flask-cors
# ib_insync>=0.9.86
# flask>=3.0.0
# flask-cors>=4.0.0
